export function LabelMono({ children }: { children: React.ReactNode }) {
  return (
    <div className="font-mono text-[10px] tracking-[.19em] uppercase text-dim mb-3.5">
      {children}
    </div>
  );
}
